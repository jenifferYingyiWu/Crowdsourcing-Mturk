Create a new mysql user with username 'cs_mturk' and password 'umdb2014'
by typing this in your mysql command prompt:

CREATE USER 'cs_mturk'@'localhost' IDENTIFIED BY 'umdb2014'
GRANT ALL PRIVILEGES ON * . * TO 'cs_mturk'@'localhost'

Run create_database.sql to create the cs_mturk database and members table.

Start webserver (e.g. xampp) and open webpage to localhost/mturk/login.html

Website Structure
-----------------
login --> home --> uploadFiles --> collectParameters --> createHIT --\       
 L--> register _____|       L----> viewResults -----------------------\  home
 L--> recovery              L----> changeKeys ------------------------/     
                            L----> logout ---------------------------/      
